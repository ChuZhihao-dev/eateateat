# 个人选餐应用：生产级工程化建设计划

## 1. 目标与边界

### 目标

在不改变“帮助个人决定今天吃什么”的产品定位下，将项目建设为：

- 可长期维护：清晰分层、类型安全、低耦合、易扩展；
- 可稳定运行：异常可控、数据可恢复、升级不丢数据；
- 可验证交付：自动化测试、质量门禁、可重复部署；
- 可在个人设备上良好使用：移动优先、弱网可用、数据可导出。

### 本期业务边界

- 单一个人的菜品库、冰箱食材、偏好、推荐、用餐记录和统计；
- 本地优先运行；云同步是后续可选能力；
- 不引入企业组织、多人协作、订餐、供应链、支付或食堂运营能力。

### 非目标

- 不为了“企业级”而过早拆分微服务；
- 不在数据量和协作规模尚小时强制迁移 PostgreSQL；
- 不重写现有 UI 框架。第一阶段保留原生前端，优先治理结构和质量。

## 2. 建设原则

1. **单体优先，边界清楚**：保持一个可独立部署的应用，通过模块边界而非微服务解决复杂度。
2. **本地优先，用户掌控数据**：SQLite 继续作为默认存储；备份、导入、导出和恢复优先于账号体系。
3. **接口是契约**：所有外部输入在边界校验；前后端共享或生成类型，避免字段漂移。
4. **先正确、可观测，再优化**：先覆盖错误处理、测试、日志和备份，再做性能与推荐策略升级。
5. **渐进式重构**：每阶段可独立发布、可回滚，不阻塞现有功能使用。

## 3. 目标架构

```text
浏览器 / PWA
  └─ UI 页面、状态、API 客户端、离线队列
       ↓ HTTP（版本化 API）
Express 应用（模块化单体）
  ├─ routes        路由与认证/限流等横切能力
  ├─ controllers   请求解析与响应映射
  ├─ schemas       Zod 输入/输出契约
  ├─ services      菜品、记录、统计、偏好、推荐等业务用例
  ├─ domain        推荐评分与领域规则（可单测、无 HTTP/DB 依赖）
  ├─ repositories  SQLite 数据访问与事务
  └─ infrastructure 日志、配置、迁移、备份、健康检查
       ↓
SQLite（迁移版本管理 + WAL + 备份）
```

建议迁移到 TypeScript，但仍使用 Node.js + Express + SQLite。推荐算法应保持纯函数化，数据库查询和随机抽样通过依赖注入隔离，便于测试和迭代。

## 4. 分阶段实施计划

## 阶段 0：基线与风险收敛

**目的**：在改动业务前建立可回归的安全基线。

### 工作项

- 固化 Node 版本（`.nvmrc` 或 Volta）及 npm 版本；补充 `.env.example`。
- 增加 ESLint、Prettier、EditorConfig 和统一 npm scripts。
- 配置单元测试和 API 集成测试框架（建议 Vitest + Supertest）。
- 补充 CI：安装、格式检查、Lint、测试、依赖审计。
- 增加健康检查接口：`GET /health`（进程存活）与 `GET /ready`（数据库可用）。
- 统一错误响应格式：`code`、`message`、`requestId`；生产环境不返回堆栈或原始数据库错误。
- 记录当前 API 的契约快照和关键用户流程，作为回归基线。

### 验收标准

- 全新环境可依据 README 在固定 Node 版本启动。
- 每次提交均自动执行 lint 与测试；失败不可合并。
- 健康检查能分别反映服务和数据库状态。
- 现有推荐、菜品 CRUD、记录、统计行为未发生非预期变化。

## 阶段 1：后端分层、类型与数据校验

**目的**：消除输入不可信、文件职责过重和静默数据异常。

### 工作项

- 将 `server.js` 按模块拆分为路由、控制器、服务、仓储和中间件。
- 迁移后端至 TypeScript；定义 `Dish`、`Meal`、`Preference`、`Recommendation`、`Stats` 等领域类型。
- 使用 Zod 对 path/query/body 参数做校验和转换。
- 对以下规则实施服务端校验：
  - ID 为正整数；
  - `meal_type` 仅允许 breakfast/lunch/dinner；
  - `rating` 为 null 或 1–5 整数；
  - `spice` 为 0–3 整数；
  - 价格、耗时为有限的非负数，并设置合理上限；
  - 数组内容去重、去空格、限制长度；
  - 查询参数 `count`、`limit`、`avoidDays` 有明确范围。
- 修复“`Number(value) || 默认值` 把合法 0 当成空值”的问题。
- 统一日期策略：数据库统一存 UTC ISO 时间；显示和月度统计按用户时区转换，避免月初错月。
- 为写操作设置事务边界，并建立数据库迁移表与版本化迁移脚本。

### 验收标准

- 非法参数返回稳定的 4xx 错误码和可展示消息，且不会写入脏数据。
- 新旧 API 在合法请求下保持兼容或提供明确版本迁移说明。
- 迁移脚本可从空库和当前 `app.db` 重复执行且安全。
- 单元测试覆盖所有校验边界和时间边界。

## 阶段 2：个人数据可靠性与生命周期

**目的**：让长期使用的个人数据可恢复、可迁移、可追溯。

### 工作项

- 菜品采用软删除/归档（`archived_at`），历史用餐记录永久保留。
- 增加 `updated_at`，关键记录增加来源与创建时间；必要时写入简洁审计事件。
- 实现数据导出：版本化 JSON（完整备份）和 CSV（菜品/用餐记录）。
- 实现数据导入：先校验、预览冲突、显式确认，再事务导入；失败时完整回滚。
- 实现本地自动备份策略：启动前检查、按日保留、可配置保留天数；提供恢复说明。
- 引入幂等键或服务端去重规则，避免网络重试/连点产生重复用餐记录。
- 提供“清空数据”与“恢复初始示例数据”能力，并要求二次确认和备份提示。

### 验收标准

- 删除菜品后，历史统计和记录仍能正确展示菜品快照。
- 任意一次导出均可导入到空库并还原核心数据。
- 导入异常、进程中断或重复提交不产生半完成数据。
- 用户能不依赖开发者安全备份和迁移个人数据。

## 阶段 3：推荐质量与个人偏好

**目的**：让推荐从“随机筛选”进化为可解释、可配置的个人助手。

### 工作项

- 新建个人偏好：忌口、过敏原、不想吃食材、饮食目标、预算默认值、偏好菜系与辣度。
- 将忌口/过敏原作为硬过滤条件，永远不被“逐级放宽”突破。
- 将推荐过程拆为：候选过滤 → 特征评分 → 多样性重排 → 加权抽样。
- 补充菜品反馈：收藏、想吃、不想吃、近期不再推荐；评分仍保留。
- 输出结构化推荐解释，例如“符合减脂目标”“优先消耗临期鸡蛋”“近 7 天未吃”。
- 记录匿名化的本地推荐事件：曝光、选择、跳过、评分，用于优化规则；默认不上传。
- 建立推荐评估样例集，覆盖预算、时间、忌口、重复、空库和严格条件放宽场景。

### 验收标准

- 任一禁忌食材不会出现在推荐结果中。
- 相同输入在固定随机种子下可复现，推荐理由可追溯到规则。
- 关键推荐规则的测试覆盖率达到约定阈值（建议 90%+）。
- 用户可关闭个性化学习或清除偏好事件。

## 阶段 4：前端质量、PWA 与体验完善

**目的**：在手机上形成可靠、无障碍、弱网友好的个人工具。

### 工作项

- 将 `public/app.js` 按 API、状态、视图、组件、手势、工具模块拆分；逐步迁移 TypeScript。
- 定义前端 API Client：超时、取消、错误映射、请求重试边界和幂等请求支持。
- 移除禁止缩放的 viewport 限制；补足键盘操作、焦点管理、aria 标签、触控目标尺寸和色彩对比度。
- PWA：Web App Manifest、Service Worker、静态资源缓存和离线只读模式。
- 为创建记录、编辑菜品、导入等写操作增加离线草稿与重连重试队列。
- 保存筛选条件、当前 tab、折叠状态等非敏感偏好；在服务端数据成功后再更新 UI 状态。
- 为空状态、请求失败、慢网络、权限拒绝（如陀螺仪）提供明确且可恢复的提示。

### 验收标准

- 支持浏览器缩放、键盘完整操作主要流程，并通过基础无障碍扫描。
- 断网时可访问已缓存页面，恢复网络后可安全同步待提交操作。
- PWA 可安装，常用页面二次打开速度满足既定性能目标。

## 阶段 5：发布、运行与持续改进

**目的**：使个人应用也具备可重复、可观测和可回滚的生产交付能力。

### 工作项

- 提供多阶段 Dockerfile、非 root 运行用户、`docker-compose` 示例和持久化卷说明。
- 建立环境变量白名单与启动期校验，例如端口、数据目录、备份策略、日志级别。
- 增加结构化日志、请求 ID、访问日志和关键业务事件日志，避免记录敏感原始数据。
- 增加基础指标：请求耗时、错误数、推荐耗时、数据库备份状态；接入可选监控端点。
- 配置依赖更新与安全扫描；制定升级、迁移、回滚和备份恢复 Runbook。
- 为发布建立版本号、变更日志和数据库迁移兼容性检查。

### 验收标准

- 在一台新机器可用文档和容器配置完成部署、备份和恢复演练。
- 线上错误可通过 `requestId` 定位，敏感数据不会出现在日志中。
- 发布失败可回滚应用版本，数据库迁移有明确的前进/恢复策略。

## 5. 推荐实施顺序与里程碑

| 里程碑         | 覆盖阶段 | 可交付成果                                  | 是否影响现有用户 |
| -------------- | -------- | ------------------------------------------- | ---------------- |
| M1：质量基线   | 阶段 0   | CI、测试骨架、Lint、健康检查、错误规范      | 否               |
| M2：可信 API   | 阶段 1   | TypeScript 后端、分层、校验、迁移、时间统一 | 低，需做兼容验证 |
| M3：数据可托付 | 阶段 2   | 归档、备份、导入导出、去重                  | 低，新增能力     |
| M4：更懂个人   | 阶段 3   | 偏好、禁忌、可解释推荐、反馈闭环            | 新增能力         |
| M5：可靠移动端 | 阶段 4   | 模块化前端、PWA、无障碍、弱网策略           | 中，需跨端回归   |
| M6：可发布运维 | 阶段 5   | Docker、日志、指标、Runbook                 | 否               |

建议先完成 M1 与 M2，再决定是否进入产品能力迭代。这样可以尽早把后续所有功能建立在可验证、可维护的底座上。

## 6. 质量门禁

每次合并至少满足：

- 格式化、Lint、类型检查全部通过；
- 单元测试、API 集成测试通过；
- 新业务规则有对应测试；
- 数据库结构变化必须携带 migration 和回滚/恢复说明；
- API 变化同步更新 OpenAPI 文档及前端调用；
- 不提交数据库文件、备份文件、密钥或个人数据；
- 人工检查移动端、桌面端和异常网络下的关键流程。

## 7. 当前已识别、应在 M2 前处理的问题

- 默认统计月份使用 UTC，但写入记录使用本地时间，月初可能产生错月统计。
- 价格和耗时字段以 `Number(value) || default` 处理，合法的 0 会被覆盖。
- 前端“已记录”仅存于内存，刷新丢失，服务端也没有幂等约束。
- 删除菜品会级联删除历史用餐记录。
- API 缺少对枚举、数值范围、分页参数和请求体大小的完整校验。
- 前端页面脚本集中在单文件，后续维护与测试成本会快速上升。
- viewport 禁止缩放，不符合无障碍的基本预期。

## 8. 后续再决策事项

以下内容不进入当前计划实施范围，等个人使用场景验证后再决定：

- 账号登录和跨设备云同步；
- 从 SQLite 迁移至托管 PostgreSQL；
- 接入地图、外卖或食谱第三方数据；
- AI 对话式推荐和图像识别食材；
- 家庭共享空间。

若进入云同步，应先补充隐私策略、加密方案、账号恢复流程和数据删除机制。
